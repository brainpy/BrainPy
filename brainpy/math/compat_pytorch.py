
from brainpy._src.math.compat_pytorch import (
  Tensor as Tensor,
  flatten as flatten,
  cat as cat,

  bfloat16 as bfloat16,
  half as half,
  float as float,
  double as double,
  cfloat as cfloat,
  cdouble as cdouble,
  short as short,
  int as int,
  long as long,
  bool as bool,
)
