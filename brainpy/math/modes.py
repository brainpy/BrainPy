# -*- coding: utf-8 -*-

from brainpy._src.math.modes import (
  Mode as Mode,
  NonBatchingMode as NonBatchingMode,
  BatchingMode as BatchingMode,
  TrainingMode as TrainingMode,
  nonbatching_mode as nonbatching_mode,
  batching_mode as batching_mode,
  training_mode as training_mode,
)

